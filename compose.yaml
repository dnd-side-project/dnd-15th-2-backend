name: miri

services:
  app:
    image: miri:local
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    init: true
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      SPRING_PROFILES_ACTIVE: "${SPRING_PROFILES_ACTIVE:-local}"
    stop_grace_period: 30s
