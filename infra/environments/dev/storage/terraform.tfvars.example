# 실제 값을 이 파일에 채우지 않는다. 이 파일을 복사해 `terraform.tfvars`로
# 만들고(.gitignore에 의해 커밋되지 않는다), 실제 값은 로컬 또는 CI 변수로만
# 채운다.

post_image_bucket_name = "<project_prefix>-post-images-<globally-unique-suffix>"
access_log_bucket_name = "<project_prefix>-post-images-logs-<globally-unique-suffix>"
