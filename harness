#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if command -v python3 >/dev/null 2>&1; then
  exec python3 "$ROOT/scripts/harness.py" "$@"
fi

echo "python3 is required. Run: ./scripts/setup-macos.sh --install" >&2
exit 127
